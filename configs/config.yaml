project:
  name: ml-training-environment
  version: 0.1.0

paths:
  data_root: data
  raw: data/raw
  interim: data/interim
  processed: data/processed
  outputs: outputs

training:
  random_state: 42
  test_size: 0.2
  validation_size: 0.2
